set(sender_SRCS
  src/ClientConfig.cc
  src/DicClient.cc
  src/main.cc
  )

add_executable(sender ${sender_SRCS})
target_link_libraries(sender muduo_net z m)

if(NOT CMAKE_BUILD_NO_EXAMPLES)
    add_subdirectory(tests)
endif()
